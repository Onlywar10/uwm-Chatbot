// Provenance for a chat answer: the pages the retrieved context came from,
// shown to the user as an expandable "Sources" list under the reply.
export type ChatSource = { url: string; title: string };
